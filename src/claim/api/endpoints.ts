/**
 * List of endpoints.
 */
export const endpoints = {
  CLAIMS: '/api/claims',
  CLAIM_ID: '/api/claims/:id',
  REFUND_CLAIM: '/api/claims/:id/refund',
  REPLY_CLAIM: '/api/claims/:id/reply',
  FIND_ONE_BY: '/api/claims/find-one-by',
};
